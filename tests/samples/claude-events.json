{
  "brain": "claude",
  "cli": "claude 2.1.219",
  "captured": "2026-07-24",
  "invocation": "claude -p --output-format stream-json --verbose --include-partial-messages [--session-id <uuid> | --resume <id>] [--model <m>] --permission-mode <mode> --allowedTools \"<space-separated>\"  # prompt on stdin",
  "sample": "probes/samples/claude-stream.jsonl",
  "note": "Observed vocabulary, and how each line maps onto the common event model (contracts/brain.ts). A line not listed here is dropped, deliberately.",
  "vocabulary": [
    {
      "line": "system/init",
      "maps_to": null,
      "why": "capture session_id for resume; tools/model/cwd dropped"
    },
    {
      "line": "system/status",
      "maps_to": null,
      "why": "requesting/idle churn \u2014 ops noise, not audit"
    },
    {
      "line": "system/thinking_tokens",
      "maps_to": null,
      "why": "reasoning meter; usage is taken once from result"
    },
    {
      "line": "rate_limit_event",
      "maps_to": null,
      "why": "account state, not turn activity"
    },
    {
      "line": "stream_event/message_start",
      "maps_to": null,
      "why": "framing"
    },
    {
      "line": "stream_event/content_block_start",
      "maps_to": null,
      "why": "framing"
    },
    {
      "line": "stream_event/content_block_delta + delta.type=text_delta",
      "maps_to": "text_delta",
      "why": "the live stream \u2014 the only source of assistant text"
    },
    {
      "line": "stream_event/content_block_delta + delta.type=thinking_delta",
      "maps_to": "thinking",
      "why": "reasoning, where the account and request produce any. Every delta in the captured stream carries an empty string, so the shape is proven and the content is not \u2014 an empty one is dropped"
    },
    {
      "line": "stream_event/content_block_delta + delta.type=input_json_delta",
      "maps_to": null,
      "why": "half-parsed tool args; the whole input arrives on the assistant line"
    },
    {
      "line": "stream_event/content_block_stop",
      "maps_to": null,
      "why": "framing"
    },
    {
      "line": "stream_event/message_delta",
      "maps_to": null,
      "why": "carries a partial usage delta; counting it too would double-count"
    },
    {
      "line": "stream_event/message_stop",
      "maps_to": null,
      "why": "framing"
    },
    {
      "line": "assistant + content[].type=text",
      "maps_to": null,
      "why": "CRITICAL DEDUPE \u2014 repeats text already streamed as deltas"
    },
    {
      "line": "assistant + content[].type=tool_use, name=TodoWrite",
      "maps_to": "todo_update",
      "why": "items = input.todos[] -> {text: content, done: status=='completed'}",
      "status": "NO LIVE PRODUCER \u2014 TodoWrite is absent from claude 2.1.219's headless tool list (see docs/clarify.md \u00a71)"
    },
    {
      "line": "assistant + content[].type=tool_use, name=AskUserQuestion",
      "maps_to": "clarify_request",
      "why": "id = tool_use.id; options = input.questions[0].options[].label",
      "status": "NO LIVE PRODUCER \u2014 absent from the headless tool list, and allowlisting it does not make the model use it. A real question arrives as text_delta and the turn ends; the answer lands by resuming the session (see docs/clarify.md \u00a71-2)"
    },
    {
      "line": "assistant + content[].type=tool_use (any other name)",
      "maps_to": "tool_call",
      "why": "id/name passed through; input stays `unknown`"
    },
    {
      "line": "user + content[].type=tool_result",
      "maps_to": "tool_result",
      "why": "id = tool_use_id, ok = !is_error, summary = truncated content"
    },
    {
      "line": "result/success | result/error_*",
      "maps_to": "usage, then turn_done",
      "why": "usage.input = input_tokens + cache_creation (what was new); usage.cached = cache_read, reported separately \u2014 a captured turn showed 20 fresh tokens against 302,567 cache reads, so summing them reports a number that is real and misleading; turn_done.ok = !is_error"
    },
    {
      "line": "<child exits with no result line>",
      "maps_to": "turn_done{ok:false}",
      "why": "synthesised by the adapter \u2014 without it the daemon waits forever"
    }
  ]
}
