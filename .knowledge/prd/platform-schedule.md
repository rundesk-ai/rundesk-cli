---
id: SCH
name: Work that starts itself
last_verified: 2026-07-26
---

## What this is

Work rundesk begins because the time came, rather than because someone asked. A schedule says when, and
names what to start; what that turns out to be is not the schedule's concern. Schedules belong to one
gateway each, which is how one agent's schedules stay that agent's alone.

## Why it exists

- Things that should happen regularly happen without anyone remembering them.
- A schedule that could not run is never mistaken for one that did.
- What is scheduled, when it next runs, and what became of it last time are all visible at once.
- One agent's schedules are its own, and no other agent's to run or to change.

## Requirements

|  | ID | Requirement | Evidence |
|:--:|---|---|---|
| ✅ | R-SCH-1 | A schedule states when it runs in the way schedules are ordinarily stated | `a schedule is stated the way schedules are ordinarily stated`, `a schedule says lists ranges and steps`, `a day of the week is counted from sunday`, `whether a field was narrowed is what was written not what it adds up to`, `a day and a weekday together mean either one`, `a schedule nobody can understand says so`, `a schedule naming a program rather than locating it is never added` |
| ✅ | R-SCH-2 | A schedule that is due starts what it names | `a schedule that is due starts what it names`, `only what is due is due`, `a schedule naming nothing to run is never added` |
| ✅ | R-SCH-3 | What a schedule names is carried to whatever starts it, and never read on the way | `what a schedule names is carried and never read`, `a schedule naming nothing this gateway can start is reported`, `a turn can be given standing instructions from the command` |
| ✅ | R-SCH-4 | A schedule whose time passed while nothing was running is not run late | `a time that passed while nothing ran is not run late` |
| ✅ | R-SCH-5 | A schedule whose time passed while nothing was running is reported as having been passed | `what was passed over can still be counted`, `a very long absence is counted up to a point and no further`, `what fell due while nothing ran is said`, `what fell due is said after an ordinary stop and not only a crash`, `being up leaves something a later gateway can measure against` |
| ✅ | R-SCH-6 | A schedule does not begin again while what it started last time is still running | `a schedule does not begin again while the last is still running` |
| ✅ | R-SCH-7 | A schedule refused for still running is reported rather than passed over | `a schedule does not begin again while the last is still running` |
| ✅ | R-SCH-8 | Every schedule reports when it next runs, when it last ran, and what became of that | `the next time is the next one after now`, `the next time is never the moment asked about`, `the next time of a weekly schedule is found`, `a schedule that can never run says never`, `schedules are listed with when each next runs`, `changing a schedule that is not there says so`, `a day that only comes round every few years is still found`, `what each schedule last did survives a restart`, `a schedule that cannot be started says so where it can be read` |
| ✅ | R-SCH-9 | A schedule runs once for the time it is due, however often the time is examined | `a schedule runs once for the minute it is due`, `a clock stepping backwards does not run a schedule again`, `a schedule that already ran this minute does not run again after a restart`, `that a schedule fired is written down before it is run`, `a gateway coming straight back does not run the same minute again` |
| ✅ | R-SCH-10 | A schedule that cannot be understood leaves every other schedule running | `one schedule nobody can understand leaves the others running`, `something that is not a schedule at all is refused by itself`, `a schedule nobody can understand is reported and the others run`, `nothing written down is no schedules rather than a failure` |
| ✅ | R-SCH-11 | A schedule that is turned off is kept and reported, and does not run | `a schedule that is off does not run`, `a schedule that is off says so rather than a time`, `a schedule that is off is kept and shown as off`, `a schedule is turned off and on again without being lost`, `on or off has to be said as one or the other` |
| ✅ | R-SCH-12 | Deciding what is due asks nothing of the machine beyond the time | `deciding what is due asks nothing of the machine`, `a rare schedule is found without examining every minute`, `a schedule no weekday could rescue still jumps by the day` |
| ✅ | R-SCH-13 | A schedule runs only in the gateway whose agent keeps it | `a gateway runs only its own schedules`, `schedules are asked for and changed on one gateway only` |
| ✅ | R-SCH-14 | A gateway never runs, reports or alters another gateway's schedules | `a gateway runs only its own schedules`, `schedules are asked for and changed on one gateway only` |
| ✅ | R-SCH-15 | What a schedule last did, and when a gateway of its name was last up, outlive the gateway that wrote them | `what each schedule last did survives a restart`, `what fell due is said after an ordinary stop and not only a crash`, `being up leaves something a later gateway can measure against`, `that a schedule fired is written down before it is run`, `taking an agent away takes what its schedules did` , `it comes back as a moment that says which clock it is on`, `a gateway that was never up is nothing rather than a guess` |
| ✅ | R-SCH-16 | A run cut short by the gateway going is told apart from one that could not start | `a run cut short by the gateway going is not called a failure to start`, `a schedule that cannot be started says so where it can be read` |
| ✅ | R-SCH-17 | An agent's schedules that are there and cannot be read are never written over | `records that cannot be read are never read as no schedules`, `records that are not there are told from records that cannot be read`, `records that cannot be read are left exactly as they are`, `changing schedules that cannot be read says nothing was changed` |
| ✅ | R-SCH-18 | An agent's schedules that cannot be read are reported as unreadable rather than as none | `listing schedules that cannot be read never reports having none`, `a gateway whose schedules cannot be read still starts and says so`, `no schedule runs while the schedules cannot be read` |
| ✅ | R-SCH-19 | A change aimed at a schedule that is not there alters nothing and says so | `changing a schedule that is not there says so`, `a change that did change something is written` |
| ❌ | R-SCH-20 | A schedule run that already began is not begun again after the machine loses power | src/rundesk/gateway.py:204 — no test, nothing here can cut the power |
| ✅ | R-SCH-21 | A schedule can be run by hand at any time, whether or not it is due | `a schedule can be run by hand whether or not it is due` |
| ✅ | R-SCH-22 | A schedule run by hand does not change when it next falls due on its own | `running a schedule by hand leaves the time it next falls due alone` |
| ✅ | R-SCH-23 | A schedule left saying it started by a gateway that is gone is reconciled | `a schedule left saying started by a gone gateway is reconciled`, `reconciling does not move the minute it fell due`, `work the sweep found still running is left alone`, `a schedule refused by a shutdown leaves no stale start` |
| ✅ | R-SCH-24 | A schedule is never shown as running when no gateway of its name is up | `a schedule whose gateway is gone is not shown as still running`, `a schedule running right now is shown as started` |
| ✅ | R-SCH-25 | What is due, when it next runs and what was passed over agree on a schedule naming both a day and a weekday | `the next time is a weekday match the day of the month would have skipped`, `a weekday match is counted among what was passed over`, `what is due and what is next agree on every minute of a week` |
| ✅ | R-SCH-26 | Schedules are examined as soon as a gateway has its name, not an interval later | `a gateway looks at the clock as soon as it has its name`, `the first look and the ordinary tick do not start one minute twice`, `a stop asked for while it takes hold starts nothing`, `the beat still waits before saying anything` |
| ✅ | R-SCH-27 | A program a schedule starts reads the same agents the gateway starting it reads | `a program the gateway starts finds the agents the gateway is running`, `a gateway told nothing hands on what it was told itself`, `the environment says where agents are kept`, `where agents are kept is carried on when it was not passed`, `a gateway is told where agents are kept`, `a schedule run by hand is told where agents are kept` |
| ✅ | R-SCH-28 | A schedule may ask a turn rather than start a program, and what that turn did is in the agent's account | `a schedule that asks a turn completes and records it`, `a name that is already a schedules is refused rather than replaced`, `two writers claiming one name cannot both believe they got it`, `a schedule whose brain fails leaves one durable outcome`, `a schedule that asks a turn with no brain anywhere says so`, `a gateway with nothing to ask a turn with says so`, `a schedule naming both a program and a prompt is never added`, `a schedule naming nothing to run is never added` |
| ✅ | R-SCH-29 | Work the clock starts keeps a conversation of its own, and starts fresh every firing | `a schedule that names rundesk ask is admitted as the clocks work`, `a scheduled turn is never in the terminals conversation`, `two schedules on one agent are two conversations`, `each firing of a schedule starts fresh` |
| ✅ | R-SCH-30 | A turn the clock started is told that nothing asked it and nobody is waiting | `a turn the clock started is told nobody is watching`, `a turn the clock started with no schedule named still says the situation`, `what a schedule was told to say wins over rundesks own`, `a schedule names the brain that answers it` |
| ✅ | R-SCH-31 | What a schedule came to is said on the surface it names, and on no other | `what a schedule came to is said where this agent is reached`, `what a schedule came to is said on the surface it names`, `a schedule says it on the surface it names and no other`, `what the turn answered is said with what it came to`, `a schedule that started a program is still said`, `a schedule naming no surface still runs and still records`, `an agent with no channel at all still runs and still records`, `a surface nobody has spoken on has nowhere to say it`, `a surface that is not up is said rather than passed over`, `a surface that will not take it changes nothing`, `a surface that will not take it changes nothing the schedule recorded`, `a schedule says what it starts and where it reports`, `a schedule reporting to a channel this agent has not got is refused`, `a channel a schedule still reports to is not taken away`, `a channel nothing reports to is taken away`, `a schedule reporting to a channel that is not there is refused` |
| ✅ | R-SCH-32 | A schedule says which place on a surface it is reported in, and the word for one is carried unread | `a schedule says which place on a surface it reports in`, `a schedule naming no place follows the conversation`, `what a place is called is never read on the way past` |
| ✅ | R-SCH-33 | What a schedule reported on a surface is written down in the conversation it was reported in | `what a schedule reported is written down where it was reported`, `what was reported names the run that produced it` |
| ✅ | R-SCH-34 | What a surface is shown is what the agent said, and what it came to only where it said nothing | `what a surface is shown is what the agent said`, `a schedule that started a program is still said`, `a schedule that failed says so rather than saying nothing` |
| ✅ | R-SCH-35 | A schedule that has run is still one an owner can take away, and the run outlives it | `a schedule that has run can still be taken away`, `a run outlives the schedule that started it` |
| ✅ | R-SCH-36 | A schedule states either a repeating time or one moment, never both and never neither | `a schedule states a repeating time or one moment and never both`, `a schedule states a repeating time or one moment and never both or neither`, `the records themselves refuse a schedule saying when two ways`, `a row stating both a time and a moment is refused by itself`, `a schedule that runs once starts a program or asks a turn alike` |
| ✅ | R-SCH-37 | A schedule stating one moment is given that moment on the machine's own clock, never as a phrase | `a moment is given to the minute and never as a phrase`, `a moment is given as a moment and never as a phrase`, `a moment carrying a time zone is refused rather than converted`, `a moment that has already passed is refused where it is typed` |
| ✅ | R-SCH-38 | A schedule stating one moment that has run can never be due again | `a schedule that has run its one moment can never be due again`, `a clock stepping backwards does not bring one moment round again`, `a moment whose firing cannot be read back as a time is still spent`, `that the clock started one is carried off the row and never read as a time` |
| ✅ | R-SCH-39 | Running a schedule by hand never uses up the one moment it states | `running a schedule by hand never uses up the moment it states`, `a schedule whose moment has gone can still be run by hand` |
| ✅ | R-SCH-40 | A schedule whose one moment has gone is out of the listing and still in the record | `a schedule whose moment has gone is out of the listing`, `a schedule whose moment has gone is still in the record`, `a schedule whose moment has gone can still be turned off and taken away`, `an agent whose every schedule is over says which it is` |
| ✅ | R-SCH-41 | A schedule whose moment passed while nothing was running is told apart from one that ran | `one that ran is told apart from one whose moment passed unrun`, `a moment that passed while nothing ran is not run late` |

## Open questions

- Whether the time a schedule is stated in is ever anything other than the machine's own. It is the
  machine's today, which is what makes an hour that repeats a thing this has to survive rather than avoid.
- Whether two commands changing one gateway's schedules at once should wait for each other or refuse, given
  that waiting is what they do now and neither is told it waited.
