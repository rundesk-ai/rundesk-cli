---
id: ROL
name: A role, and what it hands back
last_verified: 2026-08-01
---

## What this is

A role is a shared specialist definition — what the specialty is, and the rules one
execution of it follows. A named agent hands a role one bounded task, and that task runs
as an isolated execution on the agent's behalf, without the agent's identity, memory,
history or operational rules. The named agent reviews what comes back and answers the person
who asked.

## Why it exists

- An agent can hand heavy execution to a specialist without losing what it is.
- An agent can guide, end or carry on the work it handed over, without waiting for it.
- What a worker was allowed to do, and what it ran with, is answerable a fortnight later.
- No unreviewed work reaches the person who asked for it.

## Requirements

|  | ID | Requirement | Evidence |
|:--:|---|---|---|
| ✅ | R-ROL-1 | A role is one execution by the named agent that put it on, never a durable identity | `a role definition is never listed as an agent`, `a role execution is a conversation of its own` |
| ✅ | R-ROL-2 | A role is defined by a description, a set of skills, and a posture, and nothing else | `a role is two files and the manifest holds three fields`, `a manifest field this release does not know is refused` |
| ✅ | R-ROL-3 | A role definition is completely validated before any run of it is admitted | `a role that says nothing about what it is for is refused`, `a posture that is not one is refused`, `a role with no rules has nothing to run under`, `empty rules are refused as firmly as absent ones`, `rules that reach outside the role are refused`, `a slug that is not one path component is refused`, `a role that stands somewhere else is not this installs`, `a directory missing either file is not listed as a role`, `an unusable role is refused before anything is written` |
| ✅ | R-ROL-4 | A role run is admitted only by a turn belonging to the agent it acts for | `a run is admitted by a turn belonging to the agent it acts for`, `what the records say a run was admitted with matches its bundle`, `handing work to a role needs a turn of this agents own`, `a gateway is handed everything it does about role runs`, `a turn that has already ended is not a turn that can delegate` |
| ✅ | R-ROL-5 | Every role run receives a Rundesk safety floor that the role cannot remove or widen | `a role execution is never told the named agent core rules`, `the role layers are the floor then the rules then the task`, `the floor says whose behalf this is on and refuses impersonation`, `no named agent identity memory or operating rules reach the worker`, `a role run cannot work inside the agents own home`, `a role never widens the authority its parent turn had`, `a role may still narrow what its parent turn could do` |
| ✅ | R-ROL-6 | A role run receives a bounded task brief rather than the parent agent's conversation | `the brief is the prompt and the conversation is never forwarded`, `a brief longer than the ceiling is refused`, `a run with no brief at all is refused` |
| ✅ | R-ROL-7 | A role execution stands in its target project so that project's own rules load natively | `the execution stands in the target project`, `a run with no project stands in its own locked home`, `a target that is not a directory here is refused`, `a target that is not an absolute path is refused`, `an ordinary turn stands in the agents home with its own skills` |
| ✅ | R-ROL-8 | A role run is presented every skill it names that this machine has, and none of the named agent's own | `the execution is presented the runs own locked skill snapshot`, `the skills are a set and are read back in sorted order`, `a role that names no skills is refused`, `a skill named twice is refused rather than collapsed`, `a skill this machine does not have is left out rather than refused`, `a role whose skills are all absent is still a role`, `a skill this machine has not got is simply not given`, `a skill arriving later changes what the next run is given` |
| ✅ | R-ROL-9 | A role's revision is computed from what the role is rather than maintained by its author | `reordering the skills array does not make a new revision`, `editing the rules makes a new revision`, `editing a skill the role exposes makes a new revision`, `a script losing the bit that makes it runnable is a new revision`, `adding a skill to the set makes a new revision` |
| ✅ | R-ROL-10 | What a role run was admitted with never changes for that run | `admitting locks the rules the manifest the brief and every skill`, `editing the shared role afterwards leaves this run alone`, `the role rules reach the brain exactly as they were locked`, `a roles own rules reach the brain exactly as they were written`, `a run whose locked skills were tampered with refuses rather than runs`, `a run whose locked rules were rewritten refuses rather than runs`, `a run whose locked brief was rewritten refuses rather than runs`, `a run whose locked skill content was edited refuses rather than runs` |
| ✅ | R-ROL-11 | A role run stays resumable for a retention window measured from its latest activity | `the retention window is measured from the latest activity`, `a run inside its window is still there to be carried on`, `a run a gateway was cancelled out of is left to be carried on` |
| ✅ | R-ROL-12 | Expiring a role run takes away its execution context and keeps its durable record | `expiring takes the execution context and keeps the record`, `an expired run can no longer be carried on`, `a backup holds a roles definition and a retained runs locked context` |
| ✅ | R-ROL-13 | Neither a role run nor a turn woken to review one may admit another role run | `a role run cannot admit another role run`, `a turn woken to review a handoff cannot start another role run`, `a role run cannot hand work to another role`, `the recursion marker is told to the brain as well as recorded`, `a role execution is told which run it is carrying`, `an ordinary turn is told nothing about a role` |
| ✅ | R-ROL-14 | A provider-native subagent inside a role run never settles that run | `a child agents completion does not end the parent turn`, `a subagents own conversation is not where this turn ended`, `a run that already finished is not carried again` |
| ✅ | R-ROL-15 | A terminal role outcome owes its named parent exactly one review | `a terminal outcome owes its parent exactly one review`, `offering the same terminal outcome twice still owes one review`, `a run that failed owes the same one review as one that worked`, `a review stops being owed only once it has been delivered`, `a review is left owing while the surface is down`, `a review the surface refused is left owing rather than lost`, `a role handoff is left owing when the room is already busy`, `a parent is told once and the review stops being owed`, `a gateway carries every admitted role run it finds`, `a root already in flight is never started a second time`, `a turn on no surface the agent is reachable on cannot delegate`, `a review that cannot be delivered never holds up the ones behind it`, `an undeliverable review never holds up the ones behind it`, `a brain that is no longer there ends the run and owes a review`, `anything else that goes wrong ends the run and owes a review`, `a failed run is not carried again on the next look` |
| ✅ | R-ROL-16 | Rundesk records a worker's report and asserts nothing read out of it | `the handoff is the workers own words and nothing read out of them`, `the handoff reports what the brain said it cost and nothing it did not`, `a run nothing reported a cost for says so rather than reporting none` |
| ✅ | R-ROL-17 | What a role run shows a person carries no local path | `what a role run shows carries no local path`, `a label is short safe and never the brief`, `a listing says which revision and which skills a run used`, `a listing says whether a review is still owed and how often it was tried` |
| ✅ | R-ROL-18 | Installing a release never replaces a role definition that is already there | `laying down never replaces a role that is already there`, `laying down puts every shipped role where it is missing`, `the shipped roles are read off the directory`, `every shipped role reads as a usable role`, `taking back leaves a shipped role an owner has edited`, `taking back removes a shipped role nobody has touched`, `taking back never touches a role the owner wrote`, `taking back leaves no empty directory where agents are kept`, `taking back keeps the directory an owners agents stand in` |
| ✅ | R-ROL-19 | A worker's report reaches nobody until the named parent has reviewed it | `a role handoff is never posted where a person can read it`, `a role handoff wakes the parent to review it` |
| ✅ | R-ROL-23 | A named agent can say something to a role run it is carrying, and it reaches the work in flight | `what the parent says reaches the work in flight in order`, `a word reaches one turn and never a second`, `a run carrying work is handed its parents words by the turn`, `saying something to a run that is not working says which verb was wanted`, `nothing at all is not something to say`, `a whole second task is not a word said into one`, `what is waiting to be said is something a listing can show`, `a brain that cannot be sent to refuses rather than queueing unread`, `a brain that can be sent to takes it`, `what is said before it starts is part of what it is asked`, `a refused word says nothing on the way out` |
| ✅ | R-ROL-24 | A role run a person asked to end is settled as stopped rather than carried on | `asking a running execution to stop is recorded for whatever carries it`, `a run nothing has started is settled without a provider`, `a stopped execution settles as stopped rather than carried on`, `a gateway standing down is still not a stop`, `stopping one that is already over says so rather than pretending`, `a stopped run owes its parent the same one review` |
| ✅ | R-ROL-25 | A finished role run inside its window is carried on with more work in the session it already has | `resuming puts it back in hand and asks the further task`, `the first turn is asked the brief and the next is asked what was said`, `resuming leaves the locked bundle exactly as it was`, `being asked for again starts the retention window again`, `resuming one that is still running says which verb was wanted`, `an expired run refuses all three` |
| ✅ | R-ROL-26 | Every terminal outcome a role run reaches owes its parent a review of its own | `a resumed run that finishes owes its parent another review`, `offering the same terminal outcome twice still owes one review` |
| ✅ | R-ROL-27 | Handing work to a role is shown where the work was asked for | `handing work to a role is shown where it was asked for`, `a role that could not be carried still closes its mark`, `a surface that cannot be told never holds up the work`, `a role is not called a subagent`, `a role that did not finish says so rather than that a subagent failed` |
| ✅ | R-ROL-28 | An agent can be asked what it has handed to a role, from where it is reached | `nothing handed on says so rather than answering with nothing`, `work in flight says the role the task and how long`, `a local path is never shown where other people read`, `a run that finished and was answered for is over`, `how long reads in the largest unit that is not a lie` |
| ❌ | R-ROL-20 | An authorized person may steer a running role execution from where the agent is reached | — |
| ❌ | R-ROL-21 | A role run may be shown as its named parent acting through a specialist rather than as another identity | — |
| ✅ | R-ROL-22 | A role run leaves nothing of its own in the target project | `what an adapter stood in the target is taken back when the run ends`, `taking them back works wherever a brain put them`, `nothing of the owners is taken back with them`, `a link that points anywhere else is left exactly as it is`, `a run with no project has nothing in anybodys checkout to take back`, `an expired run takes its links out of the project too` |
| ✅ | R-ROL-29 | A role run that cannot be carried is retried, and settled with the reason once a bounded number of attempts is spent | `a carry that threw once is left alone and tried again`, `a run waiting out a backoff is not offered to be carried yet`, `the ceiling settles it and says this is rundesk and not the worker`, `carrying a settled run on starts its ceiling again`, `a brain that is no longer there ends the run and owes a review`, `anything else that goes wrong ends the run and owes a review`, `a failed run is not carried again on the next look`, `a run a gateway was cancelled out of is left to be carried on` |
| ✅ | R-ROL-30 | A role run that produces no activity for the configured window is settled and its parent told | `a run that has produced nothing for the window is settled`, `a run still inside the window is left working`, `a long run that is still writing records is never settled`, `a run whose turn stopped writing records is settled`, `how long silence may last is the owners to state`, `what the install is configured with is six hours`, `a fresh install states six hours`, `an older install gains the section without losing a choice`, `an owners own window is what governs`, `a window that would settle every run at once is refused`, `something that is not a number of hours is refused`, `a missing window is refused rather than defaulted outside the file`, `a run that went quiet is settled on the sweep`, `settling a quiet run lets go of what was carrying it`, `a broken configuration never stops expired bundles being cleared` |
| ✅ | R-ROL-31 | A role run reaching the end of its retention window unsettled owes its parent a review rather than only being logged | `a run that reaches its window unsettled still owes a review`, `a run that finished properly expires with nothing new owed` |
| ✅ | R-ROL-32 | How often a review was attempted counts only attempts that reached a channel | `a parent mid turn adds nothing to the attempt count`, `the handoff still arrives once the busy parent goes idle`, `a busy parent never holds up the handoffs behind it`, `a role handoff is left owing when the room is already busy`, `a listing says whether a review is still owed and how often it was tried` |

## Open questions

- Whether a role should be grantable per agent rather than available to every named agent on the install.
- Whether a role run should be able to resolve a provider or model of its own rather than continuing the parent turn's.
- How a worker proves which turn it belongs to, given that the only identity a child process carries is one it can change.
- Where a worker's skills should be presented, given that every adapter places them beside the directory it stands in and a worker now stands in the owner's project.
