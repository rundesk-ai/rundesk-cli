---
id: ROL
name: A role, and what it hands back
last_verified: 2026-08-02
---

## What this is

A role is a shared specialist definition — what the specialty is, and the rules one
execution of it follows. A named agent hands a role one bounded task, and that task runs
as an isolated execution on the agent's behalf, without the agent's identity, memory,
history or operational rules. The named agent reviews what comes back and answers the person
who asked.

## Why it exists

- An agent can hand heavy execution to a specialist without losing what it is.
- An agent can guide, end or carry on the work it handed over, without waiting for it.
- What a worker was allowed to do, and what it ran with, is answerable a fortnight later.
- No unreviewed work reaches the person who asked for it.

## Requirements

|  | ID | Requirement | Evidence |
|:--:|---|---|---|
| ✅ | R-ROL-1 | A role is one execution by the named agent that put it on, never a durable identity | `a role definition is never listed as an agent`, `a role execution is a conversation of its own` |
| ✅ | R-ROL-2 | A role is defined by a description, a set of skills, a posture, and optionally the brain and model its runs use, and nothing else | `a role is two files and the manifest holds five fields`, `a role that pins a brain carries it in its manifest`, `a manifest field this release does not know is refused` |
| ✅ | R-ROL-3 | A role definition is completely validated before any run of it is admitted | `a role that says nothing about what it is for is refused`, `a posture that is not one is refused`, `a role with no rules has nothing to run under`, `empty rules are refused as firmly as absent ones`, `rules that reach outside the role are refused`, `a slug that is not one path component is refused`, `a role that stands somewhere else is not this installs`, `a directory missing either file is not listed as a role`, `an unusable role is refused before anything is written` |
| ✅ | R-ROL-4 | A role run is admitted only by a turn belonging to the agent it acts for | `a run is admitted by a turn belonging to the agent it acts for`, `what the records say a run was admitted with matches its bundle`, `handing work to a role needs a turn of this agents own`, `a gateway is handed everything it does about role runs`, `a turn that has already ended is not a turn that can delegate` |
| ✅ | R-ROL-5 | A role run receives the core layer and its own, never what a named agent is, and cannot remove or widen either | `a role execution is never told the named agent core rules`, `a role execution names no home no memory no channel and no rundesk command`, `a role execution reads its own rules between the floor and the task`, `the agent identity is written once and composed into the three`, `the floor says whose behalf this is on and refuses impersonation`, `no named agent identity memory or operating rules reach the worker`, `a role run cannot work inside the agents own home`, `a role never widens the authority its parent turn had`, `a role may still narrow what its parent turn could do` |
| ✅ | R-ROL-6 | A role run receives a bounded task brief rather than the parent agent's conversation | `the brief is the prompt and the conversation is never forwarded`, `a brief longer than the ceiling is refused`, `a run with no brief at all is refused` |
| ✅ | R-ROL-7 | A role execution stands in its target project so that project's own rules load natively | `the execution stands in the target project`, `a run with no project stands in its own locked home`, `a target that is not a directory here is refused`, `a target that is not an absolute path is refused`, `an ordinary turn stands in the agents home with its own skills` |
| ✅ | R-ROL-8 | A role run is presented every skill it names that this machine has, and none of the named agent's own | `the execution is presented the runs own locked skill snapshot`, `the skills are a set and are read back in sorted order`, `a role that names no skills is refused`, `a skill named twice is refused rather than collapsed`, `a skill this machine does not have is left out rather than refused`, `a role whose skills are all absent is still a role`, `a skill this machine has not got is simply not given`, `a skill arriving later changes what the next run is given` |
| ✅ | R-ROL-9 | A role's revision is computed from what the role is rather than maintained by its author | `reordering the skills array does not make a new revision`, `editing the rules makes a new revision`, `editing a skill the role exposes makes a new revision`, `a script losing the bit that makes it runnable is a new revision`, `adding a skill to the set makes a new revision` |
| ✅ | R-ROL-10 | What a role run was admitted with never changes for that run | `admitting locks the rules the manifest the brief and every skill`, `editing the shared role afterwards leaves this run alone`, `the role rules reach the brain exactly as they were locked`, `a roles own rules reach the brain exactly as they were written`, `a run whose locked skills were tampered with refuses rather than runs`, `a run whose locked rules were rewritten refuses rather than runs`, `a run whose locked brief was rewritten refuses rather than runs`, `a run whose locked skill content was edited refuses rather than runs` |
| ✅ | R-ROL-11 | A role run stays resumable for a retention window measured from its latest activity | `the retention window is measured from the latest activity`, `a run inside its window is still there to be carried on`, `a run a gateway was cancelled out of is left to be carried on` |
| ✅ | R-ROL-12 | Expiring a role run takes away its execution context and keeps its durable record | `expiring takes the execution context and keeps the record`, `an expired run can no longer be carried on`, `a backup holds a roles definition and a retained runs locked context` |
| ✅ | R-ROL-13 | Neither a role run nor a turn woken to review one may admit another role run | `a role run cannot admit another role run`, `a turn woken to review a handoff cannot start another role run`, `a role run cannot hand work to another role`, `the recursion marker is told to the brain as well as recorded`, `a role execution is told which run it is carrying`, `an ordinary turn is told nothing about a role` |
| ✅ | R-ROL-14 | A provider-native subagent inside a role run never settles that run | `a child agents completion does not end the parent turn`, `a subagents own conversation is not where this turn ended`, `a run that already finished is not carried again` |
| ✅ | R-ROL-15 | A terminal role outcome owes its named parent exactly one review | `a terminal outcome owes its parent exactly one review`, `offering the same terminal outcome twice still owes one review`, `a run that failed owes the same one review as one that worked`, `a review stops being owed only once it has been delivered`, `a review is left owing while the surface is down`, `a review the surface refused is left owing rather than lost`, `a role handoff is left owing when the room is already busy`, `a parent is told once and the review stops being owed`, `a gateway carries every admitted role run it finds`, `a root already in flight is never started a second time`, `a turn on no surface the agent is reachable on cannot delegate`, `a review that cannot be delivered never holds up the ones behind it`, `an undeliverable review never holds up the ones behind it`, `a brain that is no longer there ends the run and owes a review`, `anything else that goes wrong ends the run and owes a review`, `a failed run is not carried again on the next look`, `a review that answered nobody leaves the handoff owed`, `a review that answered marks the handoff delivered`, `a review that failed never marks the handoff delivered`, `a cancelled review never marks the handoff delivered`, `which run is reviewing is written before anything is delivered`, `the real handoff review survives a session that never read it` |
| ✅ | R-ROL-16 | Rundesk records a worker's report and asserts nothing read out of it | `the handoff is the workers own words and nothing read out of them`, `the handoff reports what the brain said it cost and nothing it did not`, `a run nothing reported a cost for says so rather than reporting none` |
| ✅ | R-ROL-17 | What a role run shows a person carries no local path | `what a role run shows carries no local path`, `a label is short safe and never the brief`, `a listing says which revision and which skills a run used`, `a listing says whether a review is still owed and how often it was tried` |
| ✅ | R-ROL-18 | Installing a release never replaces a role definition that is already there | `laying down never replaces a role that is already there`, `laying down puts every shipped role where it is missing`, `the shipped roles are read off the directory`, `every shipped role reads as a usable role`, `taking back leaves a shipped role an owner has edited`, `taking back removes a shipped role nobody has touched`, `taking back never touches a role the owner wrote`, `taking back leaves no empty directory where agents are kept`, `taking back keeps the directory an owners agents stand in` |
| ✅ | R-ROL-19 | A worker's report reaches nobody until the named parent has reviewed it | `a role handoff is never posted where a person can read it`, `a role handoff wakes the parent to review it` |
| ✅ | R-ROL-23 | A named agent can say something to a role run it is carrying, and it reaches the work in flight | `what the parent says reaches the work in flight in order`, `a word reaches one turn and never a second`, `a run carrying work is handed its parents words by the turn`, `saying something to a run that is not working says which verb was wanted`, `nothing at all is not something to say`, `a whole second task is not a word said into one`, `what is waiting to be said is something a listing can show`, `a brain that cannot be sent to refuses rather than queueing unread`, `a brain that can be sent to takes it`, `what is said before it starts is part of what it is asked`, `a refused word says nothing on the way out` |
| ✅ | R-ROL-24 | A role run a person asked to end is settled as stopped rather than carried on | `asking a running execution to stop is recorded for whatever carries it`, `a run nothing has started is settled without a provider`, `a stopped execution settles as stopped rather than carried on`, `a gateway standing down is still not a stop`, `stopping one that is already over says so rather than pretending`, `a stopped run owes its parent the same one review` |
| ✅ | R-ROL-25 | A finished role run inside its window is carried on with more work in the session it already has | `resuming puts it back in hand and asks the further task`, `the first turn is asked the brief and the next is asked what was said`, `resuming leaves the locked bundle exactly as it was`, `being asked for again starts the retention window again`, `resuming one that is still running says which verb was wanted`, `an expired run refuses all three` |
| ✅ | R-ROL-26 | Every terminal outcome a role run reaches owes its parent a review of its own | `a resumed run that finishes owes its parent another review`, `offering the same terminal outcome twice still owes one review` |
| ✅ | R-ROL-27 | Handing work to a role and what came back are each shown where the work was asked for, without a surface remembering an earlier record | `handing work to a role is shown where it was asked for`, `a role that could not be carried still says how it went`, `a surface that cannot be told never holds up the work`, `handing work to a role is one self contained record`, `a run that settled says whether it worked`, `a role record carries no path no brief and no report`, `what a role run says is not written down as the agents words`, `where a run is shown carries the role and how long it has been`, `how long it has been is what the listing says it is`, `handing work to a role is news rather than subtext`, `a settled role run shows in full with no prior handed ever seen`, `a settled run is posted after the admitting turn was cleared`, `a role run is a message of its own and never the commentary`, `a role run still shows when activity is off`, `a state this surface does not know shows nothing`, `a label carrying a path comes out through the shared guard`, `a role run carrying no role is not named after a fallback`, `a role is not called a subagent`, `a role that did not finish says so rather than that a subagent failed` |
| ✅ | R-ROL-28 | An agent can be asked what it has handed to a role, from where it is reached | `nothing handed on says so rather than answering with nothing`, `work in flight says the role the task and how long`, `a local path is never shown where other people read`, `a run that finished and was answered for is over`, `how long reads in the largest unit that is not a lie` |
| ❌ | R-ROL-20 | An authorized person may steer a running role execution from where the agent is reached | — |
| ❌ | R-ROL-21 | A role run may be shown as its named parent acting through a specialist rather than as another identity | — |
| ✅ | R-ROL-22 | A role run leaves nothing of its own in the target project | `what an adapter stood in the target is taken back when the run ends`, `taking them back works wherever a brain put them`, `nothing of the owners is taken back with them`, `a link that points anywhere else is left exactly as it is`, `a run with no project has nothing in anybodys checkout to take back`, `an expired run takes its links out of the project too`, `a run somebody stopped is taken back out of the checkout`, `a run nothing was carrying is taken back out of the checkout`, `a run that could not be carried is taken back out of the checkout`, `a run that went quiet is taken back out of the checkout`, `what the owner put there survives every one of those endings` |
| ✅ | R-ROL-29 | A role run that cannot be carried is retried, and settled with the reason once a bounded number of attempts is spent | `a carry that threw once is left alone and tried again`, `a run waiting out a backoff is not offered to be carried yet`, `the ceiling settles it and says this is rundesk and not the worker`, `carrying a settled run on starts its ceiling again`, `a brain that is no longer there ends the run and owes a review`, `anything else that goes wrong ends the run and owes a review`, `a failed run is not carried again on the next look`, `a run a gateway was cancelled out of is left to be carried on` |
| ✅ | R-ROL-30 | A role run that produces no activity for the configured window is settled and its parent told | `a run that has produced nothing for the window is settled`, `a run still inside the window is left working`, `a long run that is still writing records is never settled`, `a run whose turn stopped writing records is settled`, `how long silence may last is the owners to state`, `what the install is configured with is six hours`, `a fresh install states six hours`, `an older install gains the section without losing a choice`, `an owners own window is what governs`, `a window that would settle every run at once is refused`, `something that is not a number of hours is refused`, `a missing window is refused rather than defaulted outside the file`, `a run that went quiet is settled on the sweep`, `settling a quiet run lets go of what was carrying it`, `a broken configuration never stops expired bundles being cleared` |
| ✅ | R-ROL-31 | A role run reaching the end of its retention window unsettled owes its parent a review rather than only being logged | `a run that reaches its window unsettled still owes a review`, `a run that finished properly expires with nothing new owed` |
| ✅ | R-ROL-32 | How often a review was attempted counts only attempts that reached a channel | `a parent mid turn adds nothing to the attempt count`, `the handoff still arrives once the busy parent goes idle`, `a busy parent never holds up the handoffs behind it`, `a role handoff is left owing when the room is already busy`, `a listing says whether a review is still owed and how often it was tried` |
| ✅ | R-ROL-33 | A role may name the brain and model its runs use, and a role that names neither is unchanged in every way including its revision | `a role that pins a brain carries it in its manifest`, `a role that names neither pins nothing`, `a brain this release has never heard of is read like any other`, `a brain field that names nothing is refused`, `a model field that names nothing is refused`, `a brain that could not be one is refused when the role is read`, `what is written is kept without its surrounding space`, `pinning a brain makes a new revision`, `naming a model makes a new revision`, `a role naming neither keeps the revision it already had`, `the shipped role keeps the revision it already had`, `the locked manifest holds the brain the role named` |
| ✅ | R-ROL-34 | Which brain a role run uses is settled at admission — the flag, then the role, then the parent turn — and never resolved again | `a role that names a brain runs on it whatever the parent is on`, `a role that names neither runs on what the parent turn resolved`, `the flag beats the role`, `the role beats the parent turn`, `the parent turn beats the agents own default`, `the agents own default answers where the parent turn did not`, `a model of another brains never crosses to the one named`, `a run staying on the inherited brain keeps what that brain was given`, `a role edited after admission does not move the brain it started on`, `a run admitted before a run recorded its brain is carried as it was`, `a run recorded on another brain is given none of the parents`, `a listing says which brain a run actually used`, `which brain a run used is never a local path`, `a run admitted before a brain was recorded shows none`, `which brain a run uses is asked for on the way in`, `showing a run says which brain it actually used`, `a run admitted before a brain was recorded says nothing about one`, `the refusal names the brain that cannot be sent to` |
| ✅ | R-ROL-35 | A role run whose brain this machine has not got is refused when it is admitted, naming the brain and the agent | `a brain this agent has not got is refused at admission`, `an agent with no brain at all is refused at admission`, `which brain it runs on is asked of the machine it would run on`, `a brain that is no longer there ends the run and owes a review` |
| ✅ | R-ROL-36 | A role run still working says so where the work was asked for, on a fixed cadence, until it settles | `a run inside its first window owes nothing`, `a run past the window owes that check in`, `a second look inside the same window owes nothing more`, `a gateway that restarted mid run resumes where the clock is`, `an elapsed that could not be answers nothing rather than raising`, `a run inside its first window is not checked in on`, `a run past its window says which check in it has reached`, `a run already told about in this window owes nothing more`, `a run this agent never admitted is never checked in on`, `a run still working says so once per window`, `a run inside its window is never checked in on`, `a run this gateway is not carrying is never checked in on`, `a check in a surface cannot take is never said again`, `a surface that is down is skipped without raising`, `a settled run stops being checked in on`, `a run still working says how long it has been`, `a check in says how long the run has been going`, `a check in edits the message it already posted for that run`, `a check in posts afresh once something else has been said`, `two runs in one conversation never share a check in`, `a check in that could not be edited is posted instead`, `a run that settled stops being checked in on` |
| ✅ | R-ROL-37 | A handoff that cannot be reviewed after a bounded number of attempts is settled and the owner told | `a handoff under the ceiling is woken for again`, `a handoff at the ceiling is settled once and the owner told`, `the undeliverable notice repeats no word of the unreviewed report`, `a surface that could not tell the owner leaves the handoff owed`, `a handoff settled undeliverable never holds up the ones behind`, `the notice names the run the role and how often it was tried`, `the notice says it is rundesk reporting rather than the worker`, `the notice has nowhere a report a path or a brief could go`, `a handoff is offered a bounded number of times` |

| ✅ | R-ROL-38 | Every role a release ships ends its rules in a numbered definition of done, which is what a parent reviews an unchecked report against | `every shipped role ends in a numbered definition of done`, `every shipped role reads as a usable role` |

| ✅ | R-ROL-39 | Creating a role writes both files or neither, refuses an existing slug, and answers with the rules path and the requirement to specialize it | `writing a role leaves the two files a role is made of`, `a role that is already there is never written over`, `a directory holding one of the two files says which is missing`, `a refused write leaves neither the role nor the half of one`, `a write that could not finish leaves nothing behind`, `a slug that could not be one says the shape a slug takes`, `a posture that is not one is refused before anything is written`, `skills that are not a set at all are refused`, `a description over the limit says the limit and the length given`, `a role a command wrote survives taking the release back`, `writing a role answers with the rules path and the work left in them`, `a role is written without naming an agent at all`, `a slug that is already a role is refused rather than written over`, `every refusal a role writer has reaches the exit status` |

| ✅ | R-ROL-40 | Editing a role rewrites the manifest whole or not at all, never the rules, refuses an unreadable one, and says what moved | `an edit answers with the role as it was and as it now is`, `naming no field at all is refused rather than reported as done`, `a role this install has not got is refused in the existing words`, `a manifest this release cannot read is refused rather than rewritten`, `a field a role has no such thing as is refused before anything moves`, `the skills given replace the whole set rather than joining it`, `a field no flag named is left exactly as it was written`, `a skill this machine has not got survives an edit of something else`, `an empty brain unpins one by taking the field out of the file`, `unpinning a brain puts the role back at the revision it had`, `every refusal a manifest has is refused here too`, `a replace that could not finish leaves the manifest it landed on`, `no edit of any kind touches the rules`, `editing a role this release ships is allowed and detaches it`, `the answer names both revisions and only the fields that moved`, `the answer says the change lands on the next run`, `the answer says the rules were not touched and where they stand`, `an empty brain unpins one rather than naming nothing`, `editing a role this release ships says what it costs`, `editing a role somebody wrote says nothing about a release`, `an edit that asked for what was already true says nothing moved`, `a role this install has not got is refused rather than written`, `every refusal an edit has reaches the exit status` |

| ✅ | R-ROL-41 | Writing or editing a role names no agent, and a listing shows the install's library with one agent's runs beneath it when named | `a role is written without naming an agent at all`, `editing a role names no agent and needs none to exist`, `the library is listed with nobody named at all`, `naming an agent lists the same library and that agents runs`, `every verb about a run is still typed after the agent`, `which actions name an agent is read off the parser` |

| ✅ | R-ROL-42 | A first word reading both as an agent's name and as a roles action is refused, naming both readings, rather than resolved to one | `an agent named after an action is refused rather than guessed`, `an action is an action where no agent answers to that name` |

| ✅ | R-ROL-43 | A settled role run says whether it succeeded, was stopped or failed, and a stop names who asked when that is known | `who asked for a stop is written down with the ask`, `a stop nobody named a source for records nobody`, `a word this release does not know is kept as nobody`, `a second ask never rewrites whose decision the first one was`, `carrying a stopped run on forgets who stopped it`, `a stop typed at a terminal and one asked inside a turn are told apart`, `a run somebody stopped says it was stopped and who asked`, `a run stopped before anybody was written down names nobody`, `a run that worked and one that failed each say which`, `a run nothing has settled has reached no ending at all`, `a run stopped before the column existed is carried forward naming nobody`, `a stop recorded once the column exists keeps who asked`, `a run somebody stopped is shown as stopped and says who asked`, `a run nothing has settled yet is shown with no ending at all`, `a settled run says which of the three endings it reached`, `an ending nothing settled is no ending rather than a word for one`, `a stop says who asked and says nothing where nobody wrote it down`, `who asked is never beside an ending nobody asked anything for`, `a run somebody stopped reads as a decision rather than a fault`, `a stop says which of the two it was`, `a stop nobody was written down for names nobody`, `a settled record from a rundesk that names no ending still renders`, `the stopped mark is a glyph and never slacks name for a reaction` |
| ✅ | R-ROL-44 | Guiding a role run and carrying one on are each shown where the work was asked for, without repeating what was said | `a steer into a role run is shown as its own message`, `a steer into a role run never repeats what was said`, `a role run carried on says so and drops the role word when there is none`, `neither a steer nor a resume into a role run carries an elapsed clause`, `a steer and a carrying on are each shown where the person asked`, `a word said into a run is shown once each where the work was asked for`, `a run carried on is shown as carried on rather than handed over`, `how much has ever been said to a role is told apart from what is waiting` |

## Open questions

- R-ROL-20 is closed rather than pending: the owner has decided a person never reaches a running role directly, and the named agent steers it on their behalf, which is R-ROL-23 and already works.
- Whether a role should be grantable per agent rather than available to every named agent on the install.
- How a worker proves which turn it belongs to, given that the only identity a child process carries is one it can change.
- Where a worker's skills should be presented, given that every adapter places them beside the directory it stands in and a worker now stands in the owner's project.
