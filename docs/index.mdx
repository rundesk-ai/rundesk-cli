---
title: Rundesk
description: Run AI coding agents as durable, named teammates on your own Mac — then reach them from your terminal, Discord, or a schedule.
template: splash
tableOfContents: false
editUrl: false
next: false
prev: false
hero:
  # Starlight renders the hero title with `set:html`, so the wordmark itself can
  # stand where the name would be. Served from `public/` rather than the asset
  # pipeline, since frontmatter carries a string and not an import.
  title: '<img class="rd-hero-word" src="/rundesk-wordmark.png" alt="rundesk" width="1106" height="222">'
  tagline: Teammates that remember, adapt, and grow. Run the coding CLI you already use as a durable, named agent on your own Mac.
  actions:
    - text: Get started
      link: /start/what-rundesk-is/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/rundesk-ai/rundesk-cli
      icon: github
      variant: minimal
  image:
    html: |
      <div class="rd-term" role="img" aria-label="A terminal session creating an agent named ava, asking it a question, and starting its gateway.">
        <div class="rd-term__bar">
          <span class="rd-term__dot"></span><span class="rd-term__dot"></span><span class="rd-term__dot"></span>
          <span class="rd-term__name">ava — rundesk</span>
        </div>
        <pre class="rd-term__body"><code><span class="l"><span class="p">$</span> rundesk add ava <span class="f">--provider</span> codex</span>
      <span class="l ok">✓ ava is ready — home, memory, skills</span>
      <span class="l"><span class="p">$</span> rundesk ask ava <span class="s">"what looks risky?"</span></span>
      <span class="l dim">ava · codex · thinking…</span>
      <span class="l">3 commits today. One worth a look:</span>
      <span class="l"><span class="a">api.py:88</span> drops the auth guard.</span>
      <span class="l"><span class="p">$</span> rundesk start ava</span>
      <span class="l ok">✓ gateway up — live on Discord<span class="rd-term__cursor"></span></span></code></pre>
      </div>
---

import { Card, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';
import Install from '@site/components/Install.astro';

<Install />

<div class="rd-pills">
	<span class="rd-pill"><span class="rd-pill__dot"></span> macOS</span>
	<span class="rd-pill">Python 3.9+</span>
	<span class="rd-pill">No build step</span>
	<span class="rd-pill">MIT</span>
</div>

Rundesk keeps the coding CLI you already use — **Codex**, **Claude Code**, **Grok**, **Google
Antigravity** — running with its own workspace, rules, memory, skills, conversations, and
history. It does not replace them. It gives them a dependable home and a common operating
layer.

<CardGrid>
	<Card title="Get started" icon="rocket">
		Install Rundesk and bring up your first agent in a few minutes.

		[Start here →](/start/what-rundesk-is/)
	</Card>
	<Card title="Understand the model" icon="open-book">
		Agents, gateways, homes, and the four files that decide who an agent is.

		[Concepts →](/concepts/agents/)
	</Card>
	<Card title="Reach it from anywhere" icon="comment">
		Put an agent on Discord, or give it a schedule that runs while you sleep.

		[Guides →](/guides/discord/)
	</Card>
	<Card title="Extend it" icon="puzzle">
		Adapters are executables, so another brain or another platform is yours to add.

		[Extend →](/extend/provider-adapters/)
	</Card>
</CardGrid>

## Quick start

<Steps>

1. **Install.** No shell profile is edited, and everything lands under `~/.rundesk`.

   ```sh
   curl -fsSL https://github.com/rundesk-ai/rundesk-cli/releases/latest/download/install.sh | bash
   ```

2. **Create an agent** and check its provider can actually run.

   ```sh
   rundesk add ava --provider codex
   rundesk doctor ava
   ```

3. **Talk to it.** The answer streams back, and the next `ask` resumes the conversation.

   ```sh
   rundesk ask ava "review this repository and tell me the highest-risk open issue"
   ```

4. **Keep it available** after the terminal closes. `launchd` owns the gateway from here.

   ```sh
   rundesk start ava
   ```

</Steps>

[The full walkthrough is in Start here →](/start/first-agent/)

## What Rundesk adds

Coding agents are excellent at a turn of work, but their native home is one terminal session
that ends when you close it. Rundesk adds the parts needed to operate them over time.

<CardGrid>
	<Card title="One identity, one home" icon="document">
		Every agent has its own workspace, rules, memory, skills, conversations, and logs, and
		keeps them across turns, surfaces, and restarts.
	</Card>
	<Card title="Always available" icon="approve-check">
		macOS `launchd` owns each agent's gateway and brings it back after a crash, reboot, or
		automatic update. One gateway per agent, managed independently.
	</Card>
	<Card title="Terminal, chat, and cron" icon="random">
		Continue the same conversation from the command line, from Discord, or from a
		schedule. It is the same agent either way.
	</Card>
	<Card title="Bring your own brain" icon="setting">
		Four provider CLIs ship first-class. Rundesk uses the login already on your machine and
		never copies provider credentials.
	</Card>
	<Card title="A durable account of every turn" icon="document">
		Messages, tool activity, outcomes, errors, and token usage recorded per turn, in
		Rundesk's own form rather than a provider's private session format.
	</Card>
	<Card title="Local and recoverable" icon="approve-check">
		Program and data live in separate directories. Manual and daily backups, explicit
		restore, and no hosted server required.
	</Card>
</CardGrid>

## Browse the docs

<CardGrid>
	<LinkCard
		title="Start here"
		description="What Rundesk is, install, and your first agent."
		href="/start/what-rundesk-is/"
	/>
	<LinkCard
		title="Concepts"
		description="Agents, gateways, the agent home, conversations, and skills."
		href="/concepts/agents/"
	/>
	<LinkCard
		title="Guides"
		description="Discord, schedules, and backups."
		href="/guides/discord/"
	/>
	<LinkCard
		title="Reference"
		description="Providers, and every command generated from the parser."
		href="/reference/providers/"
	/>
	<LinkCard
		title="Extend"
		description="Provider adapters, channel adapters, and integration CLIs."
		href="/extend/provider-adapters/"
	/>
	<LinkCard
		title="Source"
		description="The repository, its issues, and every release."
		href="https://github.com/rundesk-ai/rundesk-cli"
	/>
</CardGrid>

## Requirements

- macOS
- Python 3.9 or newer
- At least one [supported provider CLI](/reference/providers/), installed and signed in

Rundesk itself is standard-library Python with no build step. It is
[MIT licensed](https://github.com/rundesk-ai/rundesk-cli/blob/main/LICENSE).
