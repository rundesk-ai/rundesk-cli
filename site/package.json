{
  "name": "rundesk-docs",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npm run generate && astro dev",
    "start": "npm run generate && astro dev",
    "generate": "npm run sync:cli && npm run build:llms",
    "sync:cli": "node scripts/sync-cli-reference.mjs",
    "build:llms": "node scripts/build-llms.mjs",
    "prebuild": "npm run generate",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.4",
    "@astrojs/starlight": "^0.41.5",
    "astro": "^7.0.2",
    "sharp": "^0.34.5",
    "starlight-sidebar-topics": "^0.8.0"
  },
  "private": true,
  "description": "Renders docs/ into docs.rundesk.ai"
}
