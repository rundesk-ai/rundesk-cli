# scripts/

Keep small agent-owned automation that saves repeated work. Make its purpose, inputs, and safety
limits clear; test it before relying on it, and remove it when obsolete. Keep project scripts in
their project and never store secrets here.
