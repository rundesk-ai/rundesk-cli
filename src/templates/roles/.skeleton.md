# {label}

{description}

**TODO — this file is the generic skeleton `rundesk roles add` wrote, and none of it is about
this specialty yet.** The line above is the description a parent reads while deciding whether to
delegate at all; write it out here as one or two sentences naming **the weight of the work**
rather than only its subject — a change too large for the parent to hold, a sweep across hundreds
of sites, a subsystem that has to be held in view all at once. A role described by its subject is
one that gets handed two-file edits. Say here which instruction files beat these rules where the
work has its own. Then delete this paragraph and answer every TODO below.

## Start here, in this order

TODO — numbered, in the order a run actually reads them: the brief and any plan it names, then
the project's own rules, then the skills that match what is in front of it. End with what the run
settles in one line each before it starts, and what it does when the brief did not say.

## While you work

TODO — the few rules this kind of work gets wrong without being told, and why each one is there.
`Do X, because Y tends to cause Z` survives pressure that `ALWAYS DO X` does not.

## The ceiling

TODO — what is a stop rather than a judgement call in this specialty, and what a report of being
blocked has to name so the parent can unblock it without asking.

## Subagents

TODO — what one of the brain's own subagents is worth using for here, and what may never be
handed to one.

## The report

TODO — the named sections the parent is owed, one line each, so an unfamiliar report is reviewed
in the same shape as every other one.

## Definition of done

TODO — numbered claims somebody who was not there can check. `The command that proves it was run
and its output is in the report — a failure verbatim.` is one of those; `Make sure the work is
properly tested and high quality.` is not.

1. TODO — the first thing that must be true, in words that can be checked.
2. TODO — as many more as this specialty needs, and this line deleted.
3. The report is true about what you did not do.
