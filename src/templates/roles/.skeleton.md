# {label}

{description}

**TODO — this file is the generic skeleton `rundesk roles add` wrote, and none of it is about this
specialty yet.** The `{description}` line above is what a parent reads while deciding whether to
delegate at all; write it there rather than here, as one or two sentences naming **the weight of the
work** — a change too large for the parent to hold, a sweep across hundreds of sites, a subsystem
that has to be held in view all at once. A role described by its subject is one that gets handed
two-file edits, so keep selection criteria out of this file: the execution reading it has already
been chosen. Replace this paragraph with the precedence line — which instruction files beat these
rules where the work has its own, and that neither those nor these lift the ceiling or anything else
in the role execution rules above — then answer every TODO below.

## Start here, in this order

TODO — numbered, in the order a run actually reads them: the brief and any plan it names, then the
project's own rules, then the skills. Say **both** kinds when you write the skills step: the ones
matching the stack in front of it, and the one governing whatever the run *ends in* — a pull request,
a plan, an issue, a release each have one, and a step that names only house style is a step a run
satisfies by opening the language skill and nothing else. End with what the run settles in one line
each before it starts, and what it does when the brief did not say.

## While you work

TODO — the few rules this kind of work gets wrong without being told, and why each one is there.
`Do X, because Y tends to cause Z` survives pressure that `ALWAYS DO X` does not.

Two bullets belong here in every role, last:

- **The ceiling this role narrows to**, and the actions that are a stop rather than a judgement call
  in this specialty. The floor already says the brief is the whole authority and that a stop is
  reported `blocked` with the action and what it was for; write only what this role forbids beyond
  it, and name the actions rather than describing them.
- **What a subagent is worth here**, and what may never be handed to one. The floor already grants
  subagents and requires one task, your own limits, and a definition of done. Write only the reading
  this specialty delegates, and the thing that is never delegated because it is the deliverable
  itself.

## The report

TODO — the named sections the parent is owed, one line each, so an unfamiliar report is reviewed in
the same shape as every other one. The first must be the outcome in one word, and a partial outcome
must be required to name what is unfinished and why.

End the section with what to send when the run is blocked before that report can exist: `blocked`
alone, naming the action and what it was for, and none of the sections above.

## Definition of done

TODO — numbered claims somebody who was not there can check. `The command that proves it was run and
its output is in the report — a failure verbatim.` is one of those; `Make sure the work is properly
tested and high quality.` is not.

1. TODO — every part of the brief is delivered or reported blocked, in this specialty's own terms:
   a count that reconciles, a requirement mapped to a task, a named behaviour proven. This item is
   what stops a run finishing half the work honestly, so write it first and make it arithmetic where
   the work allows.
2. TODO — as many more as this specialty needs, and this line deleted.
3. The report is true about what you did not do.
