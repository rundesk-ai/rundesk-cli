"""rundesk — a lightweight, provider-agnostic multi-agent gateway.

The version here is the one source of it: the CLI reports it, the updater compares
against it, and a release tag is expected to match.
"""

__version__ = "0.1.1"
