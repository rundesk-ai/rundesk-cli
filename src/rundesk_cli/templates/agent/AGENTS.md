# {{name}}

Rules for {{name}}. This file is loaded before anything is asked of it, because of where
it stands and what it is called.

## Read these first

Before answering, read `SOUL.md`, `USER.md` and `MEMORY.md` in this directory, and follow
them:

- `SOUL.md` — what this agent is for, and how it works.
- `USER.md` — who it works for.
- `MEMORY.md` — what it has learned that is worth keeping.

They are named here rather than linked, and asked for rather than pointed at: a provider
loads this file because it stands in this directory under this name, and none of them
follows a Markdown link on its own.

## Where it works

`workspace/` is where {{name}} works when nothing says otherwise.
